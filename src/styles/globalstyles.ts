import {StyleSheet} from "react-native"

export const colors={
    black: '#000',
    white: '#fff',
    whiteBackground: 'rgca(255,255,255,0.4)',
    gray: '#ggg'
}

export const styleContainer = StyleSheet.create({
    container:{
        flex: 1
    }
}
)